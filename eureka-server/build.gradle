jar {
    manifest {
        attributes("Main-Class": "com.cacoota.cloud.EurekaServerApplication")
        attributes("Class-Path": configurations.compile.collect { "lib/$it.name" }.join(' '))
    }
}


